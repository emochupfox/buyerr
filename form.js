function buyHijab() {
    const hijabType = document.getElementById('hijabType').value;
    const hijabColor = document.getElementById('hijabColor').value;
    const quantity = document.getElementById('quantity').value;
    const paymentMethod = document.getElementById('paymentMethod').value;

    // Simulate purchase process (replace with actual logic)
    console.log(`Purchasing ${quantity} ${hijabColor} ${hijabType}(s) via ${paymentMethod}`);
    
    // Display confirmation message (optional)
    alert(`Thank you for your purchase! You have bought ${quantity} ${hijabColor} ${hijabType}(s) via ${paymentMethod}.`);
}
function buyHijab() {
    const hijabType = document.getElementById('hijabType').value;
    const hijabColor = document.getElementById('hijabColor').value;
    const quantity = document.getElementById('quantity').value;
    const paymentMethod = document.getElementById('paymentMethod').value;

    // Simulate purchase process (replace with actual logic)
    console.log(`Purchasing ${quantity} ${hijabColor} ${hijabType}(s) via ${paymentMethod}`);
    
    // Display confirmation message (optional)
    alert(`You have added the product to your cart`);
}
